services:
  - type: web
    name: fastapi-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.server:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: MONGO_URL
        value: "mongodb+srv://ilitecode_db_user:LiteCode@1222@cluster0.c90tk5x.mongodb.net/?appName=Cluster0"
      - key: DB_NAME
        value: "Interview-Schedular"
      - key: ADMIN_USERNAME
        value: "admin"
      - key: ADMIN_PASSWORD
        value: "admin123"
      - key: CORS_ORIGINS
        value: "*"
        - key: PORT
        value: 10000


